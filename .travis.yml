language: node_js
node_js:
  - "stable"
  - "iojs"
before_install:
  - sudo apt-get update
  - sudo apt-get install gcc
  - sudo apt-get install build-essential
  - BUILD_ONLY=true npm install nodegit
